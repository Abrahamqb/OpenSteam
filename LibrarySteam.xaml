<Window x:Class="OpenSteam.LibrarySteam"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="OpenSteam Library" Height="500" Width="450"
        WindowStyle="None" WindowStartupLocation="CenterOwner"
        AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True">

    <Grid Margin="15">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.7" Color="Black"/>
        </Grid.Effect>

        <Border x:Name="MainBorder" CornerRadius="20" Background="#0F0F0F" BorderThickness="1" BorderBrush="#33FFFFFF" ClipToBounds="True">
            <Border.OpacityMask>
                <VisualBrush>
                    <VisualBrush.Visual>
                        <Border Background="Black" CornerRadius="20" 
                                Width="{Binding ElementName=MainBorder, Path=ActualWidth}" 
                                Height="{Binding ElementName=MainBorder, Path=ActualHeight}"/>
                    </VisualBrush.Visual>
                </VisualBrush>
            </Border.OpacityMask>

            <Grid>
                <Canvas IsHitTestVisible="False">
                    <Ellipse Fill="#1A007ACC" Width="300" Height="300" Canvas.Left="-100" Canvas.Top="-100">
                        <Ellipse.Effect>
                            <BlurEffect Radius="80"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Fill="#1A6A2B9A" Width="300" Height="300" Canvas.Right="-100" Canvas.Bottom="-100">
                        <Ellipse.Effect>
                            <BlurEffect Radius="80"/>
                        </Ellipse.Effect>
                    </Ellipse>
                </Canvas>

                <StackPanel Margin="25" VerticalAlignment="Center">
                    <TextBlock Text="INSTALLED LUA" Foreground="White" FontSize="26" FontWeight="ExtraBold" HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="10" Color="#007ACC" ShadowDepth="0" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="Manage your active patches" Foreground="#007ACC" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2,0,20"/>

                    <Border Background="#0D1117" CornerRadius="15" BorderThickness="1" BorderBrush="#1AFFFFFF" Height="230" Padding="5">
                        <ListBox x:Name="LuaListBox" 
                                 Background="Transparent" 
                                 BorderThickness="0" 
                                 Foreground="White" 
                                 SelectionMode="Extended" 
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="ItemBorder" CornerRadius="10" Padding="12,8" Margin="2" Background="Transparent" BorderThickness="1" BorderBrush="Transparent">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Path Data="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M13,9V3.5L18.5,9H13Z" 
                                                              Fill="#007ACC" Stretch="Uniform" Width="14" Margin="0,0,12,0"/>
                                                        <ContentPresenter VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#22007ACC"/>
                                                        <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#44007ACC"/>
                                                        <Setter Property="Foreground" Value="#007ACC"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#0FFFFFFF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Border>

                    <TextBlock Text="Tip: Hold CTRL to select multiple" Foreground="#33FFFFFF" FontSize="9" HorizontalAlignment="Center" Margin="0,8,0,15"/>

                    <UniformGrid Columns="2">
                        <Button x:Name="BtnOpenSteam" Content="🚀 Open in Steamdb" Height="48" Margin="5" Cursor="Hand" Click="BtnOpenSteam_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="b" CornerRadius="12" BorderThickness="1" BorderBrush="#33FFFFFF">
                                        <Border.Background>
                                            <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                                <GradientStop Color="#1E1E1E" Offset="0"/>
                                                <GradientStop Color="#2D2D30" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="b" Property="BorderBrush" Value="#007ACC"/>
                                            <Setter TargetName="b" Property="Background" Value="#33FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Foreground>White</Button.Foreground>
                        </Button>

                        <Button x:Name="BtnDelete" Content="🗑 Delete Selected" Height="48" Margin="5" Cursor="Hand" Click="BtnDelete_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="b" CornerRadius="12" BorderThickness="1" BorderBrush="#44FF4444">
                                        <Border.Background>
                                            <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                                <GradientStop Color="#FF4444" Offset="0"/>
                                                <GradientStop Color="#880000" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="b" Property="Background" Value="#FF6666"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Foreground>White</Button.Foreground>
                        </Button>
                    </UniformGrid>
                    <Button Content="Back to Dashboard" Click="Back_Click" Background="Transparent" BorderThickness="0" Foreground="#44FFFFFF" Cursor="Hand" Width="110"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>