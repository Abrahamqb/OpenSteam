<UserControl x:Class="OpenSteam.LibrarySteam"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True">
    <Grid>
        <Canvas IsHitTestVisible="False"/>

        <StackPanel Margin="30" VerticalAlignment="Center">
            <TextBlock Text="INSTALLED LUA" Foreground="White" FontSize="28" FontWeight="ExtraBold" HorizontalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="10" Color="#007ACC" ShadowDepth="0" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="Manage your active patches" Foreground="#007ACC" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2,0,25"/>

            <Border Background="#0D1117" CornerRadius="15" BorderThickness="1" BorderBrush="#1AFFFFFF" Height="250" Padding="5">
                <ListBox x:Name="LuaListBox" Background="Transparent" BorderThickness="0" Foreground="White" SelectionMode="Extended" ScrollViewer.HorizontalScrollBarVisibility="Disabled" FontFamily="Arial Rounded MT Bold">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="ItemBorder" CornerRadius="10" Padding="12,8" Margin="2" Background="Transparent" BorderThickness="1" BorderBrush="Transparent">
                                            <StackPanel Orientation="Horizontal">
                                                <Path Data="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M13,9V3.5L18.5,9H13Z" Fill="#007ACC" Stretch="Uniform" Width="14" Margin="0,0,12,0"/>
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#22007ACC"/>
                                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#44007ACC"/>
                                                <Setter Property="Foreground" Value="#007ACC"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ItemBorder" Property="Background" Value="#0FFFFFFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>

            <TextBlock Text="Tip: Hold CTRL to select multiple" Foreground="#33FFFFFF" FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,20"/>

            <UniformGrid Columns="2">
                <Button x:Name="BtnOpenSteam" Content="Open in Steamdb" Height="48" Margin="5" Cursor="Hand" Click="BtnOpenSteam_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" CornerRadius="12" BorderThickness="1" BorderBrush="#33FFFFFF">
                                <Border.Background>
                                    <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                        <GradientStop Color="#1E1E1E" Offset="0"/>
                                        <GradientStop Color="#2D2D30" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="#007ACC"/>
                                    <Setter TargetName="b" Property="Background" Value="#33FFFFFF"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Foreground>White</Button.Foreground>
                </Button>

                <Button x:Name="BtnDelete" Content="Delete Selected" Height="48" Margin="5" Cursor="Hand" Click="BtnDelete_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" CornerRadius="12" BorderThickness="1" BorderBrush="#44FF4444">
                                <Border.Background>
                                    <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                        <GradientStop Color="#FF4444" Offset="0"/>
                                        <GradientStop Color="#880000" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#FF6666"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.Foreground>White</Button.Foreground>
                </Button>
            </UniformGrid>
        </StackPanel>
    </Grid>
</UserControl>