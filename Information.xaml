<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="OpenSteam.Information"
        Title="OpenSteam Information" Height="480" Width="450"
        WindowStartupLocation="CenterOwner" Background="Transparent" 
        WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize"
        MouseDown="Window_MouseDown"
        
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="8" BorderThickness="1" BorderBrush="#33FFFFFF"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#44FFFFFF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ExitButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="0,20,0,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="10">
        <Grid.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.6" Color="Black"/>
        </Grid.Effect>

        <Border x:Name="MainBorder" 
                Background="#0F0F0F" 
                CornerRadius="20" 
                BorderThickness="1" 
                BorderBrush="#33FFFFFF"
                ClipToBounds="True">

            <Border.OpacityMask>
                <VisualBrush>
                    <VisualBrush.Visual>
                        <Border Background="Black" 
                                CornerRadius="20" 
                                Width="{Binding ActualWidth, ElementName=MainBorder}" 
                                Height="{Binding ActualHeight, ElementName=MainBorder}"/>
                    </VisualBrush.Visual>
                </VisualBrush>
            </Border.OpacityMask>

            <Grid ClipToBounds="True">
                <Canvas IsHitTestVisible="False" VerticalAlignment="Top" HorizontalAlignment="Left">
                    <Ellipse Fill="#1A007ACC" Width="300" Height="300" Canvas.Left="-100" Canvas.Top="-100">
                        <Ellipse.Effect>
                            <BlurEffect Radius="80"/>
                        </Ellipse.Effect>
                    </Ellipse>
                </Canvas>

                <StackPanel Margin="25">
                    <TextBlock Text="OpenSteam Framework" Foreground="White" FontSize="24" FontWeight="ExtraBold" Margin="0,0,0,5">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="8" Color="#007ACC" ShadowDepth="0" Opacity="0.4"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <TextBlock x:Name="InfoVersion" Text="Version 2.1 Beta" Foreground="#66FFFFFF" FontSize="11" Margin="0,0,0,20"/>

                    <ScrollViewer VerticalScrollBarVisibility="Hidden" Height="337" Margin="0,0,0,15" Cursor="ScrollNS">
                        <StackPanel>
                            <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" FontSize="13" LineHeight="20" Margin="0,0,0,20"><Run Text="OpenSteam is an open-source tool designed to enhance user freedom within the Steam ecosystem. It acts as a compatibility and patching layer that allows the injection of advanced features."/></TextBlock>
                            <Border Background="#15FFFFFF" CornerRadius="10" Padding="15" 
                                BorderThickness="1" BorderBrush="#1AFFFFFF" SnapsToDevicePixels="True">
                                <StackPanel>
                                    <TextBlock Text="Dev" Foreground="#007ACC" FontWeight="Bold" FontSize="13" Margin="0,0,0,5"/>
                                    <TextBlock 
                                        TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="12" Text="Open source project created by Jbrequi (Abrahamqb) . Github: https://github.com/Abrahamqb/OpenSteam/"/>
                                </StackPanel>
                            </Border>

                            <TextBlock Text="🛠️ Core Features" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,12,0,12"/>
                            <Border Background="#15FFFFFF" CornerRadius="10" Padding="15" Margin="0,0,0,10" 
                                    BorderThickness="1" BorderBrush="#1AFFFFFF" SnapsToDevicePixels="True">
                                <StackPanel>
                                    <TextBlock Text="Library Jailbreak" Foreground="#007ACC" FontWeight="Bold" FontSize="13" Margin="0,0,0,5"/>
                                    <TextBlock Text="Unlocks the Steam library, allowing you to play games natively, including titles loaded via Lua." 
                                               TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="12"/>
                                </StackPanel>
                            </Border>

                            <Border Background="#15FFFFFF" CornerRadius="10" Padding="15" Margin="0,0,0,10" 
                                    BorderThickness="1" BorderBrush="#1AFFFFFF" SnapsToDevicePixels="True">
                                <StackPanel>
                                    <TextBlock Text="LuaLoader" Foreground="#007ACC" FontWeight="Bold" FontSize="13" Margin="0,0,0,5"/>
                                    <TextBlock Text="Loads Lua files from external servers to access non-DRM titles. Note: Online play requires an Online Fix patch." 
                                               TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </StackPanel>
            </Grid>
        </Border>
        <Rectangle x:Name="UpperLimit" HorizontalAlignment="Center" Height="2" Margin="0,85,0,0" Stroke="#66FFFFFF" VerticalAlignment="Top" Width="380" RenderTransformOrigin="0.5,0.5" Fill="#66FFFFFF">
            <Rectangle.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-0.018"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Rectangle.RenderTransform>
        </Rectangle>
        <Rectangle x:Name="ButtonLimit" HorizontalAlignment="Center" Height="2" Margin="0,435,0,0" Stroke="#66FFFFFF" VerticalAlignment="Top" Width="380" RenderTransformOrigin="0.5,0.5" Fill="#66FFFFFF">
            <Rectangle.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-0.018"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Rectangle.RenderTransform>
        </Rectangle>

        <Button x:Name="ExitButton" Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
         Width="45" Height="32" Style="{StaticResource ExitButtonStyle}" Click="ExitButton_Click" />

    </Grid>
</Window>